# Synthetic samples

Run `scripts/create-synthetic-samples.py` from the project virtual environment to create:

- `synthetic-payslip.pdf`
- `synthetic-timing-screenshot.png`

Both contain fictional data only. `setup-paytracker.ps1` creates them automatically.

